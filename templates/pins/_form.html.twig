{{ form_start(form) }}
    {{ form_widget(form) }}

    {% if '/edit' in url %}
        <input type="hidden" name="_method" value="PUT">
    {% endif %}
    <input type="submit" value="{{ submitButtonText }}" class="btn btn-primary col-12" formnovalidate>
{{ form_end(form) }}