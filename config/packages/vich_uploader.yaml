vich_uploader:
    db_driver: orm

    mappings:
        pin_image:
            uri_prefix: /uploads/pins
            upload_destination: '%kernel.project_dir%/public/uploads/pis'
            namer: Vich\UploaderBundle\Naming\SmartUniqueNamer
